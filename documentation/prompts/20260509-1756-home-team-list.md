# Home page — team list for CLAPIERS BASKET BALL

## What was done

Replaced the placeholder home page with a list of teams for club "CLAPIERS BASKET BALL", each linking to its team page.

## Modified files

- `backend/src/team/team.controller.ts` — added `GET /teams?club=<name>` route with `@Query('club')`
- `backend/src/team/team.service.ts` — added `findByClub(clubName)`: queries teams by `club.name`, ordered by category then suffix, returns id/name/category/gender
- `frontend/src/pages/Home.tsx` — replaced hello-world with a fetch to `/teams?club=CLAPIERS+BASKET+BALL`; renders a bordered list of team rows (name + category·gender) each linking to `/teams/:id`

## Recreate from scratch

> Add `@Get()` to `TeamController` with `@Query('club')` param calling `teamService.findByClub`. Add `findByClub(clubName: string)` to `TeamService`: `repo.find({ where: { club: { name: clubName } }, relations: ['club'], order: { category: 'ASC', suffix: 'ASC' } })`, map to `{ id, name, category, gender }`. Replace `Home.tsx`: fetch `/teams?club=CLAPIERS+BASKET+BALL`, render a `border border-border rounded-xl` divided list of `Link` rows showing team name and `category · gender` meta.
